name: CI pipeline to automate build

on:
  push:
    branches:
      - main
      - dev
    paths-exclude:
      - '**/*.md'
      - 'assets/'
      - 'public/'
  
permissions:
  contents: read

jobs:

  security-scan:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0
      
      - name: Install dependencies
        run: npm ci
      
      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: '22'
      
      ### SCA: Secrets scan

      - name: Secret scan
        uses: gitleaks/gitleaks-action@v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_NOTIFY_USER_LIST: '@harshitrajsinha'

      ### Dependency scan:

      - name: Dependency scan
        uses: oke-py/npm-audit-action@v4.0.2
        with:
          audit_level: high
          github_token: ${{ secrets.GITHUB_TOKEN }}
          issue_assignees: harshitrajsinha
          issue_labels: vulnerability,test
      - name: Display audit report
        run: npm audit --audit-level=high || true
      
      ## File system scan:
      
      - name: Scan file system
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: table
          exit-code: 1
          severity: HIGH,CRITICAL

      ### SAST: 

      - name: 
        uses: SonarSource/sonarqube-scan-action@v8.1.0
        with:
          args: >
            "-Dsonar.projectName=nike-devops"
            -Dsonar.projectKey=nike-devops
            -Dsonar.sources=src
            -Dsonar.verbose=true
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}


      ### SBOM generation:

      - name: SBOM generation
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'fs'
          format: 'github'
          output: 'dependency-results.sbom.json'
          scan-ref: '.'
          skip-setup-trivy: true

  linting:
    if: github.ref == 'refs/heads/dev'
    needs: [security-scan]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      statuses: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0
      
      - name: Install dependencies
        run: npm ci
      
      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: '22'
      - name: Super-Linter
        uses: super-linter/super-linter@v8.6.0
        with:
          DEFAULT_BRANCH: 'dev'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # unit-tests:

  # image-build-release:
  #   needs: [security-scan]
  #   runs-on: ubuntu-latest

  #   permissions:
  #     contents: read

  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v6.0.2
      
  #     - name: Install dependencies
  #       run: npm ci
      
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v6.4.0
  #       with:
  #         node-version: '22'
      
  #     - name: Build app
  #       run: npm build